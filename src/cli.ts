// Interactive CLI — see spec §9.
//
// Accepts feedback as either letters ("gy..g") or digits ("21002"). Prints
// the next guess and remaining candidate count after each turn.

/**
 * TODO(spec §9): readline loop, prompt formatting, parse feedback strings.
 */
export async function playInteractive(): Promise<void> {
  throw new Error('playInteractive: not implemented (spec §9)');
}
